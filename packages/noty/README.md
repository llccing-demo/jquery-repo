# `noty`

> jQuery提示插件

## Usage

```js
function show(type, text, theme = 'nest') {
      new Noty({
        theme: 'nest',
        type,
        text,
      }).show();
    }
```

## Install

```
yarn install
```